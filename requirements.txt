# cellscope_analysis runtime deps.
# Already satisfied by the CellScope `cellpose4` conda env — these pins are
# for creating a standalone environment if you prefer one.
numpy>=1.24
tifffile>=2023.0
pyqtgraph>=0.13
PyQt5>=5.15
# dev / tests
pytest>=7.0
